<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\UltimatePoKeSync.App\UltimatePoKeSync.App.csproj" />
    <ProjectReference Include="..\..\src\UltimatePoKeSync.Parsing\UltimatePoKeSync.Parsing.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The same real-RAM capture the parser is pinned against. -->
    <None Include="..\UltimatePoKeSync.Parsing.Tests\Fixtures\emerald-it-treecko.json"
          Link="Fixtures\emerald-it-treecko.json"
          CopyToOutputDirectory="PreserveNewest" />
    <!-- And the Gen 5 one, for the generation whose recommendations do not exist yet. -->
    <None Include="..\UltimatePoKeSync.Parsing.Tests\Fixtures\black-it-snivy.json"
          Link="Fixtures\black-it-snivy.json"
          CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
