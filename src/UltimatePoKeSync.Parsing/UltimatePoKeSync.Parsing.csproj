<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Turns raw bytes into Pokemon. Knows about PKHeX.Core, as does
    GameData.Learnsets (D-027); the invariant is that Analysis, Contracts and the UI
    never do, so the binary format and the GPL dependency stay out of them. See D-007.
  -->

  <ItemGroup>
    <PackageReference Include="PKHeX.Core" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\UltimatePoKeSync.Contracts\UltimatePoKeSync.Contracts.csproj" />
  </ItemGroup>

</Project>
