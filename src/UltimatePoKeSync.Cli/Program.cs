namespace UltimatePoKeSync.Cli;

internal static class Program
{
    private static Task<int> Main(string[] args)
    {
        Console.WriteLine("UltimatePoKeSync CLI — scaffolding. Implementazione in M2/M3.");
        return Task.FromResult(0);
    }
}
